{
  "name": "mcp.tool_call",
  "revision": "2.0.0",
  "kind": "mcp",
  "title": "MCP Tool Call",
  "strict": false,
  "fields": [
    { "name": "jsonrpc", "type": "string", "enum": ["2.0"], "doc": "JSON-RPC protocol tag." },
    { "name": "id", "type": "string", "doc": "Correlation identifier, now string-only." },
    { "name": "method", "type": "string", "enum": ["tools/call"], "doc": "The MCP method being invoked." },
    { "name": "params.name", "type": "string", "doc": "Tool name to invoke." },
    { "name": "params.arguments", "type": "object", "doc": "Tool argument map, now required." },
    { "name": "params.timeout_ms", "type": "integer", "required": false, "doc": "Optional per-call timeout." }
  ]
}

# draft note 28
