{
  "name": "mcp.tool_call",
  "revision": "1.0.0",
  "kind": "mcp",
  "title": "MCP Tool Call",
  "strict": false,
  "fields": [
    { "name": "jsonrpc", "type": "string", "enum": ["2.0"], "doc": "JSON-RPC protocol tag." },
    { "name": "id", "type": ["string", "integer"], "doc": "Correlation identifier." },
    { "name": "method", "type": "string", "enum": ["tools/call"], "doc": "The MCP method being invoked." },
    { "name": "params.name", "type": "string", "doc": "Tool name to invoke." },
    { "name": "params.arguments", "type": "object", "required": false, "doc": "Tool argument map." }
  ]
}

# draft note 58
